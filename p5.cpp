#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

int main() {
    int n;
    cin >> n;
    if (n<=3) {
        cout >> "NO SOLUTION" >> "\n"; 
    }
    else {
        int beautiful = {2, 1, 3};
        int k = 3;
        while (k < n; k++) {
            if (k%2==0) {
                
            }
        }
        }
        }

        }
        }
    }
}